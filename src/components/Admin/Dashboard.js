import * as React from "react";
import { Card, CardContent, CardHeader } from '@material-ui/core';

const Dashboard = () => (
    <Card>
        <CardHeader title="Administration home page" />
        <CardContent>sugar, spice and everything nice</CardContent>
    </Card>
);

export default Dashboard;